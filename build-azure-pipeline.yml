pool:
  vmImage: ubuntu-16.04

steps:
- task: DockerInstaller@0
  displayName: 'Install Docker 17.09.0-ce'

- task: Docker@2
  displayName: buildAndPush
  inputs:
    containerRegistry: 'service-containers'
    repository: 'testes-exemplo'
    tags: |
     $(Build.BuildId)
     latest